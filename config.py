# ============================================================
# 네이버 지도 실내지도 다운로드 자동화 - 설정 파일
# ============================================================

# 검색할 건물명
BUILDING_NAME = "신촌세브란스병원"

# 실내지도 층 (왼쪽 패널에 표시되는 텍스트 그대로)
FLOOR = "B3"

# ============================================================
# 영역 지정 (픽셀 좌표 기준)
#
# 사용법:
#   1. python find_area.py 를 먼저 실행
#   2. 브라우저에서 실내지도 B3 진입 + 최대 확대
#   3. 캡처할 영역의 좌상단으로 이동 → Enter 누름 (좌표 저장됨)
#   4. 캡처할 영역의 우하단으로 이동 → Enter 누름 (좌표 저장됨)
#   5. 자동으로 config 값이 출력됨 → 아래에 붙여넣기
# ============================================================

# 전체 영역을 커버할 격자 수 (행 x 열)
# find_area.py 실행 후 결과값을 입력
GRID_ROWS = 4
GRID_COLS = 3

# 뷰포트 이동 시 오버랩 비율 (0.1 = 10%)
OVERLAP_RATIO = 0.2

# 다운로드 파일 저장 경로
DOWNLOAD_DIR = "./downloads"

# 다운로드 대기 시간 (초)
DOWNLOAD_WAIT = 3

# 맵 이동 후 렌더링 대기 시간 (초)
PAN_WAIT = 2

# 네이버 지도 실내지도 URL (find_area.py에서 확인 후 입력)
# 실내지도가 열린 상태의 URL을 그대로 붙여넣기
START_URL = "https://map.naver.com/p?c=20.00,0,0,0,dh"
